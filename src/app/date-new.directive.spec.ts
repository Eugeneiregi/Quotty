import { DateNewDirective } from './date-new.directive';

describe('DateNewDirective', () => {
  it('should create an instance', () => {
    const directive = new DateNewDirective();
    expect(directive).toBeTruthy();
  });
});
